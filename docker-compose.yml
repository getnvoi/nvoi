services:
  cli:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - go-cache:/root/go
      - go-mod-cache:/root/go/pkg/mod
      - bin-cache:/app/tmp
      - ${HOME}/.ssh:/root/.ssh:ro
      - ${BUILD_SOURCE:-/dev/null}:/build-source # local build source mount
      - docker-data:/var/lib/docker # persist DinD cache across runs
    privileged: true # DinD — local builder runs docker daemon + SSH tunnel in same namespace
    env_file: .env
    environment:
      - NVOI_APP_NAME=${NVOI_APP_NAME:-rails}
      - NVOI_ENV=${NVOI_ENV:-production}
      - SSH_KEY_PATH=/root/.ssh/id_rsa
      - GOBIN=/app/tmp
    working_dir: /app
    entrypoint: ["/app/bin/entrypoint"]

volumes:
  go-cache:
  go-mod-cache:
  bin-cache:
  docker-data:
