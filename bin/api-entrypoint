#!/bin/sh
set -e

BIN=/app/tmp/nvoi-api
if [ ! -f "$BIN" ] || [ "$(find /app/pkg /app/internal /app/cmd -newer "$BIN" -print -quit 2>/dev/null)" ]; then
  go build -o "$BIN" ./cmd/api >&2
fi

exec "$BIN" "$@"
