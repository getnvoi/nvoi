#!/bin/bash
set -euo pipefail

# ── Scaleway deploy via cloud CLI ─────────────────────────────────────────────
# Same topology as examples/core/scaleway/deploy, but through the API.

export COMPUTE_PROVIDER=scaleway
export DNS_PROVIDER=cloudflare
export STORAGE_PROVIDER=cloudflare
export BUILD_PROVIDER=daytona

bin/cloud login
bin/cloud workspaces use default
bin/cloud repos create dummy-rails

bin/cloud push \
  --config examples/cloud/scaleway/config.yaml \
  --env examples/.env \
  --compute-provider scaleway \
  --dns-provider cloudflare \
  --storage-provider cloudflare \
  --build-provider daytona

bin/cloud plan
bin/cloud deploy
